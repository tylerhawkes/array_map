[package]
name = "array_map_doc_test"
version = "0.0.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
array_map = { path = "..", features = ["std", "derive"] }
